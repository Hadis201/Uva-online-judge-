#include<stdio.h>

int main() {
    long long int n, ans;
    scanf("%lld",&n);
    while(n>=0){
        ans = (n*n+n+2)/2;
        printf("%lld\n",ans);
        scanf("%lld",&n);
    }
    return 0 ;
}
